{
  "name": "css-module-flow-gen-loader",
  "version": "0.1.6",
  "description": "Generates type definitions for imported css-module style files.",
  "main": "build/index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src -d build",
    "dev": "watch 'npm run build' src",
    "prepublish": "npm run build",
    "release": "np"
  },
  "author": "Joel Roxell <joel.roxell@annevo.se> (https://github.com/JoelRoxell)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-latest": "^6.24.1",
    "debug": "^2.6.8",
    "jest": "^20.0.4",
    "np": "^2.15.0",
    "watch": "^1.0.2"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/JoelRoxell/css-module-flow-gen-loader.git"
  },
  "babel": {
    "presets": [
      "latest"
    ]
  }
}
